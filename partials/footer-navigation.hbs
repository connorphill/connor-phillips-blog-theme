<ul class="inline-flex flex-wrap">
    {{#foreach navigation}}
    <li class="{{link_class for=(url) class=(concat "nav-" slug)}} px-5 py-2 md:py-0" role="menuitem"><a href="{{url absolute="true"}}" class="font-serif text-midnight-500 hover:text-midnight-500 hover:font-bold hover:underline">{{label}}</a></li>
    {{/foreach}}
</ul>